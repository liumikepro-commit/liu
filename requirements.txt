# 英语翻译 Agent 运行依赖
# 安装: pip install -r requirements.txt

# ---- Web 服务 ----
flask>=3.0.0          # Web 服务框架
gunicorn>=21.0.0      # 生产级 WSGI 服务器(云部署用)

# ---- 文档翻译(pdf / word) ----
pypdf>=4.0.0          # PDF 解析(纯 Python)
python-docx>=1.1.0    # Word .docx 解析与重建
reportlab>=4.0.0      # PDF 生成导出(需系统安装中文字体, 见 Dockerfile)

# ---- 可选: 仅构建词典脚本需要 ----
# jieba 提供中文词频, 用于候选词排序; 未安装时构建脚本自动降级
jieba>=0.42.1
